

const TREN = "Tren"
const EDIFICIO = "Edificio"

const train_length = TREN.length
const building_length = EDIFICIO.length 


if (train_length < building_length){
    console.log(TREN);
}
else {
    console.log(EDIFICIO);
}